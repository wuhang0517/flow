server:
  port: 9001

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/spring?useSSL=false&serverTimezone=GMT
    username: root
    password: 123456

mybatis:
 mapper-locations: classpath:mapper/*Mapper.xml
 type-aliases-package: com.citic.action.pojo

logging:
   level:
     com:
       citic:
         check:
            mapper: debug